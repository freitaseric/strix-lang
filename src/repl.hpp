#ifndef REPL_HPP
#define REPL_HPP

namespace strix
{
  constexpr const char *VERSION = "pre_alpha";

  /**
   * @brief Starts the Strix Read-Eval-Print loop (REPL).
   */
  void repl();
} // namespace strix

#endif // REPL_HPP